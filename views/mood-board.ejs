<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mood Board</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #111;
      color: white;
      margin: 0;
      padding: 20px;
    }

    .mood-board-container {
      text-align: center;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 20px;
    }

    .mood-card {
      background-color: #222;
      margin: 20px auto;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
      max-width: 600px;
    }

    .mood-card h2 {
      font-size: 1.5rem;
      margin: 0;
      color: #FFD700; /* Highlight text color */
    }

    .mood-card p {
      font-size: 1.2rem;
      color: #ccc;
      margin: 10px 0;
    }
  </style>
</head>
<body>
  <div class="mood-board-container">
    <h1>Your Mood Board</h1>
    <% if (moodData.length === 0) { %>
      <p>No mood data to display yet. Start posting to analyze your mood!</p>
    <% } else { %>
      <% moodData.forEach((mood) => { %>
        <div class="mood-card">
          <h2>Mood: <%= mood.mood %></h2>
          <p>Polarity: <%= mood.polarity %></p>
          <p>Subjectivity: <%= mood.subjectivity %></p>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
